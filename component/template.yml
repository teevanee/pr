apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: template
  # This is an extra long description with Markdown
  description: |
    example template for creating a service specification.
    - First item
    - Second item
  annotations:
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://github.com/swagger-api/swagger-petstore
      title: GitHub Repo
      icon: github
spec:
  type: service
  consumesApis: ['ems01']
  lifecycle: experimental
  owner:  pr-team-a
  subcomponentOf: keten01
  system: esb
